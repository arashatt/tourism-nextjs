import LoginForm from "./login-form"; // if login-form is in /app/login/
import CityList from "@/components/CityList";

export default function LoginPage() {
  return (
    <div>
      <LoginForm />
    </div>
  );
}
